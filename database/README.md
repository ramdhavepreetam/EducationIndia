# Database Directory

Historical SQL files are kept here for auditability and Supabase SQL Editor
reference. New schema changes should be made through Alembic revisions in
`backend/alembic/versions`.

See `docs/DATABASE_MIGRATIONS.md` for the migration workflow.
